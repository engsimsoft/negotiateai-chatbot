# Анализ ТЗ-MinimaxCleanup

## Резюме
Замена Haiku на MiniMax M2.7 в chatMode=simply, маршрутизация по типу сообщения (MiniMax / Gemini Flash / Sonnet), удаление saveFact/updateFact, откат скользящего окна, отключение extractAndStoreFacts.

## Вопросы для уточнения
Все вопросы согласованы (2026-04-08):

1. **updateFact — удаляем?** -> Да, удалить вместе с saveFact
2. **myProvider — вариант A или B?** -> Вариант B: модель напрямую в route.ts
3. **Tools для Gemini Flash?** -> Без tools, чисто vision-ответ
4. **Gemini 3 Flash — существует?** -> Использовать `gemini-3-flash`, без fallback
5. **System prompt saveFact блок** -> Удалить мёртвые инструкции, промпт не упрощать
6. **tool-activity-config.ts** -> Удалить записи saveFact и updateFact
7. **Контекстное окно MiniMax** -> 204,800 токенов, добавить в MODEL_CONTEXT_WINDOW
8. **Compaction для MiniMax** -> Не нужно, Extract при сжатии = следующее ТЗ

## Рекомендации разработчика (Код-ревью)

> Все рекомендации согласованы с архитектором.

### Согласен с ТЗ
- Пункты 1-9 — реализуемы, совпадают с паттернами в коде
- Порядок реализации: сначала удаления, потом добавления

### Согласовано с архитектором
| # | Было (ТЗ) | Решение | Обоснование |
|---|-----------|---------|-------------|
| 1 | Удалить saveFact | + updateFact | updateFact тоже only-simply, сирота без провайдера |
| 2 | myProvider | Вариант B (напрямую) | Проще, меньше рисков |
| 3 | Gemini tools | Без tools | Vision-only ответ |

## Оценка сложности
- [x] Простое (1-2 сессии)
