# Анализ ТЗ-BR3: Интеграция промпта аналитика

## Резюме

Три изменения в briefing-пайплайне:
1. Заменить inline-промпт в `briefing-analyzer.ts` на загрузку из `.md` файла
2. Протянуть `tier` источника в данные для аналитика через Map в `route.ts`
3. Поддержать блок `topicId: "top"` в UI (с fallback для старых брифингов)

## Вопросы для уточнения

Все вопросы заданы и отвечены:

1. **Файл промпта** — PE подготовил `briefing-analyst.md`, файл уже в `specs/TZ_BR3_PromptIntegration/`. Claude Code кладёт его в `lib/prompts/briefing/`.
2. **UI-логика "Главное"** — вариант A: если есть блок `topicId: "top"` — рендерить как есть, иначе fallback на старую логику (сборка high-items).
3. **FilteredItem.tier** — НЕ трогать. Tier нужен только Stage 2 (analyzer). Map<sourceName, tier> создаётся в route.ts.
4. **Версия** — 3.27.1 (patch).

## Рекомендации разработчика (Код-ревью)

> Ниже — технические рекомендации на основе анализа кодовой базы.

### Согласен с ТЗ
- Пункт 1 (загрузка .md) — паттерн идентичен `service-chat/route.ts:37-45`
- Пункт 2 (tier через Map) — минимум правок, tier уже есть в `briefingSources` (schema.ts:409) и `topics-catalog.ts` (RecommendedSource)
- Пункт 3 (topicId "top") — промпт PE явно описывает группировку (строка 77: `topicId: "top"`)
- Пункт 4 (что НЕ менять) — согласен, Zod-схема, пайплайн, fallback без изменений

### Рекомендовал изменить (согласовано)
| # | Было (ТЗ) | Рекомендация | Решение |
|---|-----------|--------------|---------|
| 1 | Добавить tier в filteredItemSchema | Не трогать — tier нужен только аналитику | Принято: Map в route.ts |
| 2 | UI: неявная обработка "top" | Явный fallback для старых брифингов | Принято: if/else по наличию блока "top" |

## Потенциальные риски

- **Промпт на английском, style_guide на русском** — Gemini должен корректно обрабатывать смешанный язык (проверить при тестировании)
- **Плейсхолдеры в промпте** — 6 штук (`{{LANGUAGE}}`, `{{MAX_ITEMS}}`, `{{TOPIC_REFERENCE}}`, `{{DATE}}`, `{{TOTAL_SOURCES_CHECKED}}`, `{{TOTAL_CANDIDATES}}`), нужно убедиться что все подставляются

## Зависимости

- ТЗ-BR1 (backend) — уже реализован
- ТЗ-BR2 (UI) — уже реализован
- Файл `briefing-analyst.md` — готов (в specs/)

## Оценка сложности

- [x] Простое (1 сессия)
