# Закрытые долги backlog — архивный журнал

> Исторический журнал всех долгов из `specs/_backlog/`, которые были закрыты.
> Пополняется только при закрытии долга. Источник — `specs/_backlog/README.md`.
>
> **Для подробностей** по каждому закрытому долгу — читать `HANDOFF.md` в соответствующей папке `_archive/TZ_<name>/`.
>
> Создан: 2026-04-14

---

## Журнал

| ТЗ | Закрыто в | Как |
|---|---|---|
| TZ_UsageLoggingCoverage | 2026-04-13 (TZ_UnfreezePipelines v3.86.1) | Слит в `TZ_CachePipelineMetrics` — обе задачи трогают одни и те же pipeline-файлы, раздельное выполнение было бы двойной работой |
| TZ_OpenRouterCostTracking | 2026-04-13 (v3.87.1) | Root cause оказался не namespace prefix (первая гипотеза) а version suffix от OpenRouter. Walk-back loop в `getModelEntry` делает lookup tolerant к versioned IDs |
| TZ_StreamObservability | 2026-04-14 (v3.87.2) | Server-side `onError` в обоих chat routes: console.error + emitDebugError через closure-captured writer + локализованная user-facing строка. Stage 2b расширил скоуп на recovery UX: prop-drill `clearError` из useChat → MultimodalInput |
| TZ_CreateSnapshotAudit | 2026-04-14 (v3.87.3) | SQL audit: 2 all-time calls, 0 через project task expert, 1 failed из 2. Fully deleted — 4 файла, 4 queries, 2 schema columns (migration 0054). ADR 052 «Context Management Strategy per Provider» документирует 4-уровневую стратегию |
| TZ_DeadModelSelectors | 2026-04-14 (`9ddf814`, `a1923b1`, `5b2571c`) | **Закрыто частично** (~30% scope). Удалены 3 legacy selector-файла + упрощён `entitlements.ts`. Попутно закрыт pre-existing bug override в проектных task-чатах (`9ddf814`). Остальные ~70% намеренно оставлены владельцем — `ModelSelectorCompact` в проектах сохраняется. Первая попытка Этапа 1 (коммит `772e886`) откачена `git reset --hard` после HMR incident. Внесено 2 новых находки: `prompts.ts` 90% dead (Finding #1), scattered side-effect imports (Finding #2, позже признан ошибочным) |
| TZ_OverridesReaderCentralization | 2026-04-14 (сессия 3, `fdcd9f7`, 0 коммитов в код) | **Закрыто как «ошибочно заведено»**. Finding #2 из TZ_DeadModelSelectors был сформулирован как production-concern для dev-only фичи (`/dev/models`). Попытка переноса side-effect import в `instrumentation.ts` провалилась при мануальном тесте и полностью откачена. Постскриптум добавлен в ADR 048. Правило: **перед заведением находки сверять с ADR — если ADR декларирует ограничение, отсутствие покрытия за пределами ограничения это реализация дизайна, не долг** |
| TZ_AnthropicAliasCleanup | 2026-04-14 (v3.87.5) | Follow-up к v3.87.4, заведён в рамках той же сессии. Удалены 3 мёртвых catalog entry: `title-model`, `artifact-model` (0 grep usages, dead после ТЗ-1 CoreRegistry), `claude-sonnet-4-5-20250929` (SQL audit: 2 all-time вызова, identical pricing с Sonnet 4.6, tolerant walk-back lookup сохраняет historical cost). **Live aliases оставлены:** `claude-sonnet`/`claude-haiku`/`claude-opus` — 10+ usages в UI-слое (service-chat configs, DevPanel, default model props). Архитектурное обоснование закреплено в комментариях каталога: task-assignments = физические snapshot IDs для cost precision, UI = семантические aliases для изоляции от snapshot changes. Результат: 9 Anthropic entries → 6, чистое разделение `{sonnet, haiku, opus} × {physical, alias}` |
| TZ_SimplyChatModeInjection | 2026-04-15 (v3.90.1) | Плейсхолдеры `<current_mode>` / `<current_model>` в [chat/simply-chat.md](../lib/prompts/chat/simply-chat.md) подменяются композером через SSOT (`getModelEntry(getModelIdForTask(activeTaskId))?.displayName`), а не через 2 устаревших локальных `modelMap` с Claude-псевдонимами эпохи до xAI/MiniMax. `activeTaskId` поднят в `chat/route.ts` до prompt-building (раньше композер не знал финальную модель). `buildChatPrompt/Expertise/Create` принимают опциональный `activeTaskId` вторым аргументом. Дефолт chatMode `'chat'` → `'simply'` (режим `chat` удалён в v3.86.0). Regex-replace вместо точного match — дефолты в `.md` редактируются безопасно. 5/5 мануальных тестов + бонус-подтверждение `.txt` attachment стабильности. Dead field `ComposedPrompt.model` помечен комментарием «no longer used», полное удаление уходит в будущий TZ_PromptsDeadCodeCleanup |
| TZ_ATTACH_1_PdfExtractionAtUpload | 2026-04-16 (v3.91.0, commit `dbe6bdf`) | Реализация Слоя 0 из [SIMPLY_ATTACHMENT_ARCHITECTURE.md](../specs/Simply_xAI/SIMPLY_ATTACHMENT_ARCHITECTURE.md). Shared helper `lib/pdf/extract-pdf-text.ts` через pdf-parse v2 (`new PDFParse({data}).getText()`). Эвристика scan detection: `pageCount>=2 ? avg<30 chars/page : text.length<100`. Truncate 50K chars с маркером только при реальном обрезании. Graceful fallback через catch → fall-through на Haiku. **Side-effect fix в том же коммите:** [projects/[id]/files/route.ts](../app/(chat)/api/projects/[id]/files/route.ts) использовал **v1 legacy API** (`pdfParse(buffer)` как function call) на установленном v2 package — silent catch месяцами возвращал `undefined`, `metadata.extractedContent` никогда не заполнялся. Связанный скоуп — один shared helper покрыл оба места. **3 webpack ESM мины** при интеграции: top-level static import → crash, dynamic import в функции → crash, единственное рабочее решение — `serverExternalPackages: ["pdf-parse"]` в next.config.ts (паттерн `lamejs`). **Stale cache регрессия** во время валидации: DevPanel footer пропал после серии kill/restart → `rm -rf .next && restart` починило. Валидация 4/5 (5-й симметричен сценарию 1, post-commit). Урок в NOTES: обязательный чистый rebuild после изменения serverExternalPackages |
| ТЗ-XAI-6 (Финализация серии Simply_xAI) | 2026-04-16 (v3.92.1, commit `<release-3.92.1>`) | **Символическое закрытие серии.** Scope сжался после двух correction'ов владельца: (1) MiniMax остаётся как Кухня by design (briefing pipeline), (2) OpenRouter остаётся как dev-инструмент для тестирования новых моделей. Реальные cleanup-цели оригинального плана **были уже сделаны заранее**: `stripMiniMaxToolParts` + `stripLegacyOpenAICompatToolParts` + `isSimplyNonAnthropicModel` удалены в ТЗ-XAI-3 (R-6 резолв); `snapshot-creator.ts` удалён в ADR 052. Финальный scope — 3 строки в `lib/ai/task-assignments.ts` (удалён `clerk:snapshot` TaskId placeholder) + 2 строки docs. Финальная целевая архитектура зафиксирована: **4 роли · 3 production провайдера (Grok подсобка/зал + MiniMax кухня + Anthropic автор) · 1 dev-инструмент (OpenRouter)**. Новое memory-правило `feedback_cleanup_requires_full_context.md` усиляет Rule №0: перед удалением — спрашивать про все use cases, не только production traffic. Серия Simply_xAI (ТЗ-XAI-1/2/3/4/5/6 + ATTACH-1) **закрыта**. Открытые ветки новых направлений: ТЗ-XAI-MA-1 (Multi-agent), ТЗ-XAI-COL-1 (Collections), ТЗ-XAI-VOICE-1 (Voice Agent) |
| TZ_ServiceChatNotOverridable | 2026-04-16 (briefing cleanup session, commit `<briefing-cleanup>`) | 1 реальная дыра из 3 починена, 2 оказались ложными. **Дыра 1** «UI `/dev/models` не показывает service-chat:*» — ложная, `service-chat:*` уже были в `DEFAULT_TASK_MODELS` и автоматически попадали в UI через `ALL_TASK_IDS`. Автор хвоста grep'ал по `/dev/models/` директории и не нашёл — но UI получает taskIds через import из lib/ai, прямых упоминаний в директории не должно быть. **Дыра 2** (реальная): `service-chat/route.ts` не импортировал `model-overrides-node` → +1 side-effect import line с подробным комментарием. Теперь все 4 service chats (ben, project-creation, project-manager, briefing-onboarding) переключаемые через `/dev/models` override. **Дыра 3** (docs): `docs/ai-chats-map.md` overview-таблица обновлена явными маркерами «Service chat» vs «Backend pipeline (кухня)», Briefing Onboarding section получила важное предисловие что она архитектурно независима от briefing pipeline. **Мета-урок:** хвосты с «3 дыры» чаще бывают «1 дыра + 2 предположения», при closing'е готовиться к тому что scope может быть меньше заявленного в ТЗ. **Мета-правило:** проверять claim «UI не показывает X» — если UI динамически импортирует из SSOT-списка, отсутствие match в конкретной директории не значит отсутствия функциональности |
| ТЗ-XAI-4 (Utility/Pipeline миграция + scope expansion) | 2026-04-16 (v3.92.0, commit `583ef03`) | **Не backlog-хвост, а milestone серии Simply_xAI** — запись здесь по прямой инструкции HANDOFF серии. 11 taskId на Grok в одной плотной сессии: 6 «подсобка» на Grok 4.1 Fast (briefing:filter, clerk:task-summary, clerk:file-analyzer, util:title, util:project-summary, util:artifact-suggestions) + 5 «зал» на Grok 4.20 reasoning (meeting:summary, simply-chat-think variant switch, expertise — R-5 резолв, create, memory:extract variant switch). Исходный scope 7 точек расширен Владимирскими IDE edits на 4 доп точки по empirical данным из Этапа 2 — scope ТЗ-XAI-5 (create + expertise + R-5) закрыт без отдельного релиза. **2 hot-fix pre-existing багов:** plan/route.ts + 3 briefing routes получили side-effect `import "@/lib/ai/model-overrides-node"` (reader не регистрировался → dev overrides молча игнорировались); plan/route.ts получил tactical `maxOutputTokens: 16000` под Anthropic streaming threshold 21333. **Audit metadata fix:** meeting/regenerate/route.ts hardcoded `claude-sonnet-4-6` заменён на `getModelIdForTask("meeting:summary")`. **Reserved vs deprecated семантика:** запись `grok-4.20-multi-agent-0309` осталась после снятия expertise с multi-agent variant — это RESERVED под ТЗ-XAI-MA-1, placeholder taskId `expertise-multi-agent` + 🔒 маркер в docs, не dead code. **Empirical находка (architectural, не model issue):** briefing:author 82-91% fabricated URLs на 4 моделях (Sonnet, Gemini, MiniMax M2.7, Grok 4.20). Блокирует ТЗ-XAI-6 cleanup MiniMax. Решение — `generateObject` + `z.enum([...allowedUrls])` в [TZ_BriefingAuthorUrlHallucination](../specs/_backlog/TZ_BriefingAuthorUrlHallucination.md). 7 новых backlog хвостов зафиксированы (4 из Этапов 2+3 + 3 из Этапа 4). Новое memory-правило `feedback_empirical_test_before_model_blame.md`. Папка ТЗ [_archive/TZ_xai_4_UtilityPipelines/](TZ_xai_4_UtilityPipelines/) |
| TZ_UrlVerificationMetricNormalization | 2026-04-17 (commit `bd97b73`) | Hardening follow-up к `58d9d2e`. **(1) Рефакторинг:** функция `normalizeUrlForComparison` вынесена из `lib/ai/pipeline-trace.ts` в отдельный модуль `lib/ai/url-normalize.ts` — чистый, без server-only зависимостей, тестируемый через tsx (pipeline-trace.ts re-exports для обратной совместимости). **(2) Расширен TRACKING_PARAM_REGEX:** +msclkid (Bing), +ref_src/ref_url (Twitter/X), +share_source/share_from (соц share), +si (Spotify/YouTube), +feature (YouTube), +spm/spm_* (Alibaba). **(3) Keepable регрессионный тест:** `scripts/test-url-verification-normalization.ts` — 25 кейсов (anchor, все tracking prefixes, preserve legit params, sort stability, trailing slash, www/без www, http/https, uppercase hostname, malformed fallback, control); запуск `npx tsx scripts/test-url-verification-normalization.ts`. **(4) ADR 030 обновлён** разделом «URL Verification: canonical comparison» — контракт «любое observability-сравнение URL обязано проходить через normalizeUrlForComparison, не дублировать Set.has(rawUrl)». **SQL аудит трекинг-params на реальных briefings:** выборка мала (2 brief с trace), из существующих данных встретились только utm_* (Habr) и #atom-everything (simonwillison) — оба покрыты. Расширение regex сделано проактивно по известным широкоизвестным tracking префиксам. **Валидация:** 25/25 PASS, `npx tsc --noEmit` 0 ошибок, боевой briefing `c93b70e7` 11/0/0 (verified/modified/fabricated) — до фикса `58d9d2e` на этом же классе источников было 82-91% fabricated |
| TZ_SimplyReadDocumentTool + R-6 correction | 2026-04-15 (v3.90.2) | Объединённый cleanup двух связанных проблем одним коммитом. **(1) Dead readDocument cleanup:** git audit (`62540ff`) подтвердил что папка `knowledge/` удалена ещё в v2.0.0 «cleanup: remove old MIR.TRADE files» (126 файлов); tool всегда возвращал `Access denied` независимо от режима. Удалён файл `read-document.ts` (243 строки), 4 места в `chat-tools.ts`, render block в `message.tsx` (52 строки), упоминания в 3 промптах. **(2) R-6 correction:** в ТЗ-XAI-3 я удалил `stripMediaPartsForTextModel` с неверным обоснованием «Grok умеет vision» — смешал `capabilities.vision` (image/*) с `documentSupport.supported` (application/pdf). Pre-existing bug: PDF в истории → follow-up текст → Grok crash. Правильная реализация — функция `adaptHistoryToCapabilities(messages, capabilities)` в [chat/route.ts:252-344](../app/(chat)/api/chat/route.ts#L252) через SSOT model-catalog, буквальная реализация принятого решения №3 из утверждённого 2026-04-15 [SIMPLY_ATTACHMENT_ARCHITECTURE.md](../specs/Simply_xAI/SIMPLY_ATTACHMENT_ARCHITECTURE.md). Проброшены SSOT anchor-ы: ROADMAP серии, CLAUDE.md, backlog stub ТЗ-ATTACH-1 (PDF extraction при upload — следующий ТЗ). 6/6 мануальных тестов + бонус cross-model continuity (Haiku обрабатывает PDF → follow-up на Grok через placeholder работает бесшовно). Урок: при удалении «хрупкого» кода обязательна замена через SSOT, не просто delete |
| TZ_UtilTitleCapReasoningMargin | 2026-04-20 (ТЗ-COMPACTION-1 v3.94.0, commit `accf9f8` → финализация) | **Закрыт с превышением рекомендованного значения** в рамках точечного фикса проблемы #3 ТЗ-COMPACTION-1. Smoke test обнаружил `AI_NoObjectGeneratedError` в `autoNameChat` — модель `grok-4-1-fast-non-reasoning` обрывала JSON `{title:"...", summary:"..."}` на середине строки в русском (русский ≈ 2 токена/слово, summary 1-2 предложения = 60-120 токенов **только summary**), упирался в cap=64. Поднят до **512** (вместо рекомендованного 256) — 4x запас под русский структурированный output, billing cap не actual cost. **Defense-in-depth:** `autoNameChat` обёрнут в try/catch с graceful fallback (default title уже mode-aware: «Новый запрос», «Новое задание»). Архитектурное обоснование сохраняется (cap должен учитывать запас под reasoning overhead для taskId которые могут переключаться на reasoning variant) — теперь применено к `util:title` |
| **ТЗ-COMPACTION-1 (Simply Compaction MVP)** | 2026-04-20 (v3.94.0, commits `3db0483` → `accf9f8` + финализация) | **Новый механизм управления контекстом для длинных разговоров.** Capability-driven middleware ([lib/ai/compaction/](../lib/ai/compaction/) — 6 файлов): для Anthropic Sonnet/Opus используется provider-native Compaction API, для остальных моделей (Grok, Haiku, MiniMax, GLM) — наша middleware. Триггеры: Soft 50% (100K) / Hard 85% (170K) от `SIMPLY_CONTEXT_LIMIT=200K`. Verbatim window 40K. Summary через Grok 4.1 Fast non-reasoning (`compaction:summarize`, cap 4096), 5-секционный формат (Контекст / Материалы / Решения / Фокус / Вопросы). UI индикатор встроен в существующий popover виджета контекста: 📦 «Разговор сжат» / ⚠️ «Рекомендуем начать новый запрос/задание» (mode-aware). Сам ContextIcon меняет цвет на янтарный при `truncation_warning`. **Архитектурное решение по формуле подсчёта токенов** (smoke test обнаружил расхождение ~3x): `estimateMessageTokens` теперь учитывает `tool-call`/`tool-result` parts (раньше игнорировал — это давало 2-3x занижение в expertise с активными tools); `computeToolsTokens` через `zod-to-json-schema` для tools schemas. Формула стала `system + history + new + mind + tools` (5 компонентов). Точечные фиксы: `util:title` cap 64→512 (закрывает `TZ_UtilTitleCapReasoningMargin`), `autoNameChat` try/catch, file parts учитываются в `newMessageTokens` (порядок операций), permanent observability log. **ADR 053 расширен до 5-аспектного контракта** — добавлен пятый аспект «context strategy» (контекстно-зависим, относится к chat-handler routes). **БД:** 3 колонки в Chat (compactionSummary/Index/Count), миграция 0056. **Gate:** `chatMode === "expertise" || chatMode === "create"`, Simply Chat пока вне scope. **Backlog impact:** ✅ TZ_UtilTitleCapReasoningMargin закрыт. ➕ TZ_ExpertiseCreateVisionRouting (🟥 High) — vision-routing для expertise/create на Haiku 4.5 (сканированные PDF падают). ➕ TZ_CompactionActualCalibration (🟩 Low) — sanity-check delta после недели MVP. **Урок сессии:** не отмахиваться «не наша зона» — каждая обнаруженная проблема требует root cause анализа, не костыля; архитектурные правки делегировать архитектору (отдельная Opus-сессия). **Папка ТЗ:** [_archive/TZ_COMPACTION_1/](TZ_COMPACTION_1/) |
| TZ_MindConsolidationTriggers v2 | 2026-04-21 (commit `6fed924`) | Реанимация счётчика `factsSinceConsolidation` + двойной триггер consolidation: мгновенный (`storedCount >= 10`) ИЛИ накопительный (`factsSinceConsolidation + storedCount >= 15`, новая константа `CONSOLIDATION_THRESHOLD_CUMULATIVE`). Удалена мёртвая `miniConsolidateUserMemory`. MIND_ARCHITECTURE обновлён. Без отдельной папки ТЗ — небольшой scope, делалось в составе сессии MindOnVisit prep. |
| TZ_UnifyContextThresholdBase | 2026-04-21 (закрыт ранее в ТЗ-COMPACTION-UNIFY v3.95.0, commit `969b0b4`) | **Закрыт автоматически.** Константы `EXTRACT_THRESHOLD_*` и `CONTEXT_BUDGET` удалены в ТЗ-COMPACTION-UNIFY — все пороги теперь единая база `SIMPLY_CONTEXT_LIMIT=200K`. Файл лежал в `_backlog/_archive/` но запись в README осталась ошибочно — вычищено в commit `<mind-on-visit-finalization>`. |
| **ТЗ-MindOnVisit** | 2026-04-21 (v3.96.0, commit `68fea2c`) | On-visit обработка хвостов памяти через Next.js `after()` API + выбор стратегии пользователем (`always` / `on-visit` / `cron`) в настройках памяти + расширение cron на все 4 sourceType (раньше только simply). Закрыта дыра: хвосты из expertise/create/projects больше не теряются. Best practices: Next.js `after()` (15.1+), Spring AI predicate-triggered, Mem0 async-default. Идея tiered processing вынесена в backlog как `TZ_MindDeepConsolidation`. Папка ТЗ: [_archive/TZ_MindOnVisit/](../specs/_archive/TZ_MindOnVisit/). |
| **ТЗ-ExpertiseCreateVisionRouting** | 2026-04-21 (v3.98.0) | Единый capability-driven routing вложений для всех chat modes через новый [lib/ai/routing.ts](../lib/ai/routing.ts) (`resolveActiveTaskId` + `needsVisionFallback`). Закрыто падение `AI_UnsupportedFunctionalityError` в expertise/create на сканированных PDF: Grok `documentSupport=false` → автоматический fallback на `chat-vision` (Haiku 4.5). Переименование `simply-chat-vision` → `chat-vision` (теперь универсальный fallback). **Изменение поведения simply:** картинки идут на Grok 4.1 Fast (раньше хардкод на Haiku) — экономия cost, качество подтверждено в smoke-тесте. Оба gate на `chatMode === "simply"` (`convertTextFilesInAllMessages` + `adaptHistoryToCapabilities`) сняты → один pipeline. Параллельный фикс Finding #1: `getTaskIdForTier(tier)` вместо template-string `project:expert:${tier}` в project chat (раньше писал несуществующие taskId в `ai_usage_log`). Удалён `hasAttachments()` helper. ADR 055 фиксирует capability-driven как третий SSOT-паттерн проекта. Smoke-тест 2026-04-21: CAD-чертёж в expertise → `task=chat-vision, model=haiku` ✅, text-PDF в expertise с override → `task=expertise, model=grok-4-1-fast` ✅. Папка ТЗ: [_archive/TZ_ExpertiseCreateVisionRouting/](TZ_ExpertiseCreateVisionRouting/) |
| **ТЗ-AISDKLayerHardening (umbrella: 3 долга)** | 2026-04-18 (v3.93.0, commits `a20ad29` → `da01884` + финализация) | **Umbrella ТЗ, закрывший 3 backlog-долга одним релизом:** `TZ_DevOverridesSideEffectImportAudit` (🟥 High) + `TZ_MaxOutputTokensAudit` (🟧) + `TZ_ProfessorPlanStreaming` (🟧). **Этап 1 (`a20ad29`) — Centralized overrides reader:** удалены 7 redundant side-effect импортов `model-overrides-node` из routes; единственная точка регистрации reader'а теперь в [instrumentation.ts](../instrumentation.ts) (Next.js SSOT). Reader вынесен в `globalThis.__simplyOverridesReader` — **HMR-immune** (раньше HMR пересоздавал модуль и сбрасывал module-level state в no-op, маскировалось 7 side-effect импортами). Бонус — фикс DevPanel auto-naming visibility (перенос `autoNameChat` из `createUIMessageStream.onFinish` в `streamText.onFinish` после изучения исходников AI SDK `handle-ui-message-stream-finish.ts` — safeEnqueue молча глотал late writes после TransformStream `flush()`). Diagnostic endpoint `GET /api/dev/resolve-model` без AI-вызова. **Этап 2 (`3bb23b3`) — MaxOutputTokens SSOT + safety-net + 36 call sites:** `DEFAULT_MAX_OUTPUT_TOKENS: Record<TaskId, number>` в [task-assignments.ts](../lib/ai/task-assignments.ts) (compile-time check через TS) + getter `getMaxOutputTokensForTask(taskId)` в [getModel.ts](../lib/ai/getModel.ts) с двухслойной safety-net (`Math.min(requested, capability)` + `warnOnce` при Anthropic cap > 21333). 36 production call sites мигрированы на getter (artifacts × 10, professor pipeline × 3, memory × 5, vision × 2, backend routes × 8, briefing/meeting × 4, professors/clerks × 2); единственное исключение по дизайну — `briefing:author` сохраняет dynamic `MAX_TOKENS_BY_VOLUME[volume]`. **Этап 3 (`da01884`) — Архитектурный инвариант «cap > 21333 на Anthropic ⇒ streamText»:** [plan/route.ts](../app/(chat)/api/projects/[id]/plan/route.ts) переписан с `generateText` на `streamText` для `professor:planning` (Opus cap 32000). Устранена хрупкая зависимость от 60s fetch timeout при non-streaming Opus. **Бонус — фикс `temperature` + `thinking` incompatibility в 3 файлах** (plan/route.ts, task-reviewer.ts, service-chat/route.ts): `thinking: { type: "adaptive" }` + невалидный `effort: "high"` → `thinking: { type: "enabled", budgetTokens: N }`; `temperature` вынесен в else-ветку (раньше Anthropic молча отключал thinking из-за warning'а). **ADR 053 — AI SDK invocation contract:** кодифицирует 4-аспектный контракт (taskId / model / cap / call mode) + обязательный checklist для будущих изменений. **Finding #1** → `TZ_UtilTitleCapReasoningMargin` (🟩 Low) в backlog. **Finding #2 (known limitation, не баг):** `@ai-sdk/anthropic@3.0.66` всегда возвращает `outputTokens.reasoning: void 0` ([dist/index.js:1646-1659](../node_modules/@ai-sdk/anthropic/dist/index.js#L1646)) — Anthropic Messages API не разделяет thinking от completion в usage. Следствие: `thinkingTokens` для Anthropic архитектурно всегда 0 в `ai_usage_log`. Pricing корректен (thinking уже включён в `output_tokens`). Через Professor Planning → Grok Multi-Agent (ТЗ-XAI-MA-1) проблема может стать неактуальной через 1-2 месяца. **Валидация:** 0 runaway за 24ч в `ai_usage_log`, 4 модели протестированы владельцем без обрезания и warning'ов, streamText на Opus cap 32000 за 146с без UND_ERR_SOCKET. **Урок сессии (в memory):** HMR в dev может НЕ перезапустить route handler после Edit (Next.js держит скомпилированный старый код); решение `rm -rf .next/cache && npm run dev`. **Папка ТЗ:** [_archive/TZ_AISDKLayerHardening/](TZ_AISDKLayerHardening/) |
