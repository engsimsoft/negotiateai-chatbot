# Передача сессии ТЗ-ExpertiseCreateVisionRouting

**Дата:** 2026-04-21
**Сессия:** 1 — **ЗАВЕРШЕНА**, ТЗ закрыто в v3.98.0

---

## Статус этапов

- [x] Фаза 1: Анализ — решения зафиксированы
- [x] Этап 1: SSOT rename + unified routing module (`lib/ai/routing.ts`)
- [x] Этап 2: Wire в `chat/route.ts` + снятие gate'ов (5 сценариев smoke-тест ✅)
- [x] Этап 3: ADR 055 + docs update
- [x] Этап 4: Финализация (CHANGELOG, SIMPLY_STATUS, package.json 3.98.0, backlog cleanup, коммит, архивация)

---

## Следующая сессия: начни с

1. Прочитать `ROADMAP.md` → Этап 1 целиком.
2. Получить подтверждение владельца что план утверждён (если ещё не получено).
3. Запустить `grep -rn "simply-chat-vision" lib/ app/ scripts/ --include="*.ts" --include="*.tsx"` — актуальный список call-site'ов.
4. Задача 1.1: начать с `lib/ai/task-assignments.ts` — переименование `simply-chat-vision` → `chat-vision` в 4 местах (union + 3 Record таблицы) + комментарии.

---

## В процессе

Ничего в работе — код пока не тронут. Папка содержит только artefacts Фазы 1.

---

## Ключевые артефакты сессии

- `SPEC.md` — исходник из backlog (скопирован как есть).
- `ANALYSIS.md` — полный анализ с «Изученной документацией» (Правило 1), код-ревью ТЗ, решениями, зависимостями. Переписан 1 раз после замечания владельца про унификацию (не дубли, а один механизм).
- `ROADMAP.md` — 4 этапа, gate-keeping между ними, smoke-тест 5 сценариев в Этапе 2.

---

## Блокеры / Вопросы

Нет. Все 5 стартовых вопросов (Q1–Q5) закрыты владельцем 2026-04-21:
- Haiku для всех вложений? → **Нет, строго capability-driven** (только PDF-сканы, JPG остаются на Grok).
- Оба gate? → Да, симметрично.
- Модель? → Haiku 4.5.
- chatMode в логах? → Остаётся UX-режимом, фильтр по taskId.
- project:expert? → Не требует изменений — Claude-tiers сами справляются.

---

## Риск-напоминание

⚠ `npm run build` в Этапе 2 накатывает pending migrations через `tsx lib/db/migrate`. Предупредить владельца ДО запуска.
