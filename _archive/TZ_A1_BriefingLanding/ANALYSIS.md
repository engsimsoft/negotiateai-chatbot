# Анализ ТЗ-A1: BriefingLanding

## Резюме

Две задачи в одном ТЗ:
1. **Лендинг** — переписать `/briefing` из JSON-карточного рендера в красивую продающую страницу (hero + демо + CTA)
2. **Очистка** — удалить строительные леса (каталог тем, seed, компоненты JSON-карточек)

Скоуп небольшой, UI-задача + удаление файлов. 1 сессия.

---

## Рекомендации разработчика (Код-ревью)

> Ниже — технические рекомендации на основе анализа кодовой базы.
> Каждая рекомендация требует согласования с архитектором.

### ✅ Согласен с ТЗ

- Удаление `briefing-content.tsx`, `briefing-block.tsx`, `briefing-item.tsx`, `briefing-empty.tsx`, `briefing-generate-button.tsx` — ОК. Единственный потребитель — `briefing-page.tsx`, который мы перезаписываем. Barrel `index.ts` обновим
- ~~Удаление `seed-briefing.ts` + скрипта `db:seed-briefing`~~ — оставляем (решение архитектора: может пригодиться для тестов)
- Сохранение `briefing-card.tsx` — ОК, на дашборде (`tools-section.tsx:14`) работает автономно, 3 состояния корректны
- Сохранение `briefing-header.tsx` — ОК, адаптируем: убрать счётчики (зависят от BriefingJSON), оставить навигацию + UserMenu
- Сохранение таблиц БД, фетчеров, фильтра, API — ОК
- Лендинг как Server Component в `(dashboard)` route group — ОК, паттерн header h-14 + sticky

### ⚠️ Рекомендации (СОГЛАСОВАНЫ 2026-02-19)

| # | Было (ТЗ) | Решение | Обоснование |
|---|-----------|---------|-------------|
| 1 | Удалить `topics-catalog.ts` | **✅ НЕ удаляем** — до ТЗ-А2 | Импортируется в `generate/route.ts:9` и `briefing-analyzer.ts:16`. Удаление сломает генерацию |
| 2 | Удалить fallback-логику в route.ts | **✅ НЕ трогаем** — до ТЗ-А2 | `getDefaultSources()` — единственный способ генерации пока нет онбординга |
| 3 | Удалить `seed-briefing.ts` | **✅ НЕ удаляем** — оставить для тестов | Может пригодиться пока нет онбординга |

### ❓ Уточнения (ПОЛУЧЕНЫ 2026-02-19)

1. **`/briefing/setup`** → Создать заглушку "Скоро" с кнопкой назад. 404 = плохой UX
2. **Демо-блок** → На моё усмотрение, поправки при тестировании
3. **Тексты Hero/CTA** → Пишу сам, поправки при тестировании

---

## Потенциальные риски

- **Риск 1 (снят):** Конфликт удаления `topics-catalog.ts` — если согласуем рекомендацию #1, риска нет
- **Риск 2 (низкий):** `briefing-page.tsx` сейчас принимает `briefing: BriefingJSON | null` от `page.tsx`. При переписи в лендинг этот проп не нужен, но `page.tsx` всё ещё фетчит данные. Оставлю фетч для forward-compatibility (ТЗ-А2 будет использовать для роутинга), но лендинг-компонент данные не использует

---

## Зависимости

**Что затронуто:**
- `app/(dashboard)/briefing/page.tsx` — переписать (меньше логики, рендерит лендинг)
- `components/briefing/briefing-page.tsx` — полная замена → компонент лендинга
- `components/briefing/briefing-header.tsx` — адаптировать (убрать счётчики BriefingJSON)
- `components/briefing/index.ts` — убрать экспорты удалённых компонентов
- 5 компонентов — удалить (briefing-content, briefing-block, briefing-item, briefing-empty, briefing-generate-button)
- `app/(dashboard)/briefing/setup/page.tsx` — создать заглушку

**Что НЕ затронуто (подтверждено):**
- `briefing-card.tsx`, `briefing-types.ts`, таблицы БД
- `generate/route.ts`, `latest/route.ts`
- Фетчеры, фильтр, анализатор
- `topics-catalog.ts`, `seed-briefing.ts`, `package.json` скрипт

---

## Оценка сложности

- [x] Простое (1 сессия)
